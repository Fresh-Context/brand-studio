node_modules/
.env
# Local-dev asset dirs (data/generated, data/references, data/library) and
# launchd/manual-run logs — never repo content, see src/lib/storage.js.
data/
logs/
# The bundled Identity fallback is checked in; regenerate it when the source
# artifact changes. Local data and generated logs remain ignored above.
__pycache__/
